{
  "mongo": {
    "host": "${MONGO_HOST}",
    "port": ${MONGO_PORT},
    "user": "${MONGO_USER}",
    "password": "${MONGO_PASSWORD}",
    "db": "${MONGO_DB}",
    "mechanism": "${MONGO_AUTH_MECHANISM}",
    "workerCollection": "workers",
    "workerQueue": "workers_queue"
  }
}
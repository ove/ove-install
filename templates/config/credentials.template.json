{
  "default": "${AM_STORE_NAME}",
  "stores": [
    {
      "name": "${AM_STORE_NAME}",
      "endpoint": "http://${S3_SERVER}:${S3_PORT}/",
      "proxyUrl": "http://${S3_EXT_IP}:${S3_EXT_PORT}/",
      "accessKey": "${S3_ACCESS_KEY}",
      "secretKey": "${S3_SECRET_KEY}"
    }
  ]
}
{
  "default": "${AM_STORE_NAME}",
  "stores": [
    {
      "name": "${AM_STORE_NAME}",
      "endpoint": "${S3_SERVER}:${S3_PORT}",
      "proxyUrl": "http://${S3_EXT_IP}:6081/${AM_STORE_NAME}/",
      "accessKey": "${S3_ACCESS_KEY}",
      "secretKey": "${S3_SECRET_KEY}"
    }
  ]
}

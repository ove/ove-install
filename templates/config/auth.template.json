{
  "mongo": {
    "host": "${MONGO_HOST}",
    "port": ${MONGO_PORT},
    "user": "${MONGO_USER}",
    "password": "${MONGO_PASSWORD}",
    "db": "${MONGO_DB}",
    "collection": "${MONGO_COLLECTION}",
    "mechanism": "${MONGO_AUTH_MECHANISM}"
  },
  "jwt": {
    "secret": "${JWT_SECRET}"
  }
}
